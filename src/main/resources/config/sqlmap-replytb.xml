<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="replytbDAO">
        <select id="list" resultType="replytbVO" >
                
       SELECT * FROM (SELECT ROWNUM RNUM, DATA.* FROM (SELECT * FROM REPLYTB ORDER by RENO DESC) DATA)
     	WHERE RNUM <![CDATA[ >= ]]> #{startList} AND RNUM <![CDATA[ <= ]]> #{count} 
       <!-- SELECT * FROM NOTICETB ORDER BY noticeNO DESC -->
        </select>
       
        
        <select id="getListCnt" resultType="int">
		SELECT
			count(*) as listCnt
		FROM
			REPLYTB 
		</select>
        
        
        <select id="select" parameterType="int" resultType="replytbVO">
                SELECT * FROM
                REPLYTB WHERE RENO = #{reNo}
        </select>
        
            
        <delete id="delete" parameterType="replytbVO">
                DELETE FROM REPLYTB WHERE RENO = #{reNo}
        </delete>
        
        <delete id="deleteAll">
                DELETE FROM REPLYTB
        </delete>
        

        
</mapper>
