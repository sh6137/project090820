<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="productReplyDao">

	<select id="list" parameterType="hashmap" resultType="replyVO">
		SELECT * FROM
		REPLYTB where RELOCATION=#{location} and REPOSTNO=#{postNo}
		ORDER BY RENO
	</select>
	
	<select id="insert" parameterType="hashmap" resultType="Integer">
		insert into REPLYTB
		(RENO, RELOCATION, REPOSTNO, REUSERNO, REUSER, RETEXT, REREGDT)
		values
		(REPLYTB_SEQ.nextval, #{location}, #{postNo}, #{memberNo}, #{member}, #{reText}, SYSDATE)
	</select>
	
</mapper>
