<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="boardDAO">
        <select id="list" resultType="ProductVO">
                SELECT * FROM 
        	(SELECT * FROM PRODUCT_TB ORDER BY PRO_LIKE_CNT DESC)<![CDATA[ WHERE ROWNUM <=3]]>   
        </select>
        
        <select id="list2" resultType="ProductVO">
                SELECT * FROM 
        	(SELECT * FROM PRODUCT_TB ORDER BY PRO_READ_CNT DESC)<![CDATA[ WHERE ROWNUM <=3]]>   
        </select>
</mapper>